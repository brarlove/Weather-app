import React from "react";

export default class Titles extends React.Component {
    render() {
        return(
            <div className="app-title">
                <h1>Weather App</h1>
            </div>
        );
    }
}